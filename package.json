{
  "name": "install-script",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "dependencies": {
    "nodemon": "^2.0.3"
  },
  "scripts": {
    "postinstall": "cd web && npm install",
    "deploy": "cd web && npm run deploy",
    "server-dev": "cd web && npm run server-dev"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "cacheDirectories": [
    ".next/cache/",
    "web/.next/cache/"
  ]
}
