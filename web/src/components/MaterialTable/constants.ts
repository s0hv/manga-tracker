export const cssClassPrefix = 'material-table';

export const CSS_CLASSES = {
  editCell: `${cssClassPrefix}-edit-cell`,
} as const;

export const COLUMN_IDS = {
  edit: '__edit',
  delete: '__delete',
};

export const DEFAULT_PAGE_SIZE = 25;
